name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -sS -o /dev/null -w "Status: %{http_code}\n" https://influencia-1hsp.onrender.com/app
